import 'package:flutter/material.dart';

class VetProfileManagementScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vet Profile Management'),
        backgroundColor: Color(0xFF162013),
      ),
      body: Center(
        child: Text(
          'Vet Profile Management Screen - To be implemented',
          style: TextStyle(color: Colors.white),
        ),
      ),
    );
  }
}
